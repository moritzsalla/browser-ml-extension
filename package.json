{
  "name": "chrome-extension-ml",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Moritz Salla",
  "license": "MIT",
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "prettier": "2.1.2",
    "typescript": "^4.0.2"
  },
  "scripts": {
    "format": "prettier --write .",
    "clean": "rm -r dist/ .cache/",
    "dev": "parcel src/index.html",
    "build": "parcel build --no-content-hash src/*.html src/*.ts src/*.js src/*.webmanifest --out-dir dist/chrome/"
  }
}
