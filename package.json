{
  "name": "vanilla-typescript",
  "version": "1.0.0",
  "description": "JavaScript and TypeScript example starter project",
  "scripts": {
    "clean": "rm -r dist/ .cache/ && prettier --write .",
    "build": "parcel build src/*.ts src/images/*.png && cp src/manifest.json dist/manifest.json"
  },
  "dependencies": {},
  "devDependencies": {
    "parcel-bundler": "^1.6.1",
    "prettier": "^2.1.2",
    "typescript": "^4.0.2"
  }
}
